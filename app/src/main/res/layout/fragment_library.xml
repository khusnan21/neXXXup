<?xml version="1.0" encoding="utf-8"?>

<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/library_root"
    android:background="?attr/primaryGrayBackground"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/empty_list_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="30dp"
        android:gravity="center"
        android:visibility="gone"
        tools:visibility="visible" />

    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/search_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/primaryGrayBackground">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_scrollFlags="scroll|enterAlways">

            <ImageView
                android:id="@+id/provider_selector"
                android:layout_width="25dp"
                android:layout_height="25dp"
                android:layout_gravity="end|center_vertical"
                android:layout_marginStart="10dp"
                android:background="?selectableItemBackgroundBorderless"
                android:contentDescription="@string/change_providers_img_des"
                android:src="@drawable/ic_baseline_extension_24"
                app:tint="?attr/textColor" />

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_margin="10dp"
                android:background="@drawable/search_background"
                android:visibility="visible"
                app:layout_scrollFlags="scroll|enterAlways">

                <androidx.appcompat.widget.SearchView
                    android:id="@+id/main_search"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_gravity="center_vertical"
                    android:layout_marginEnd="45dp"

                    android:iconifiedByDefault="false"
                    android:imeOptions="actionSearch"

                    android:inputType="text"
                    android:nextFocusLeft="@id/nav_rail_view"

                    android:nextFocusRight="@id/search_filter"
                    android:paddingStart="-10dp"
                    app:closeIcon="@drawable/ic_baseline_close_24"
                    app:iconifiedByDefault="false"
                    app:queryBackground="@color/transparent"
                    app:queryHint="@string/search_hint"
                    app:searchIcon="@drawable/search_icon"
                    tools:ignore="RtlSymmetry">

                </androidx.appcompat.widget.SearchView>

                <ImageView
                    android:id="@+id/library_sort"
                    android:layout_width="45dp"
                    android:layout_height="45dp"
                    android:layout_gravity="end|center_vertical"
                    android:background="?selectableItemBackgroundBorderless"
                    android:contentDescription="@string/sort"
                    android:nextFocusLeft="@id/main_search"
                    android:nextFocusRight="@id/main_search"
                    android:padding="10dp"
                    android:src="@drawable/ic_baseline_sort_24"
                    app:tint="?attr/textColor" />

                <!-- Kept for compatibility but always hidden or of 0 size -->
                <ImageView
                    android:id="@+id/list_selector"
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:visibility="gone" />

            </FrameLayout>
        </LinearLayout>

        <!-- Main Switcher: Riwayat vs Favorit -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/library_main_tab_layout"
            style="@style/Theme.Widget.Tabs"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="?attr/primaryGrayBackground"
            android:focusable="true"
            app:tabGravity="fill"
            app:tabMode="fixed"
            app:tabIndicatorColor="?attr/white"
            app:tabIndicatorGravity="bottom"
            app:tabIndicatorHeight="3dp"
            app:tabRippleColor="?attr/textColor"
            app:tabSelectedTextColor="?attr/white"
            app:tabTextAppearance="@style/TabNoCaps"
            app:tabTextColor="?attr/textColor" />

        <!-- Category tabs moved up here under Favorit switcher! -->
        <com.google.android.material.tabs.TabLayout
            android:id="@+id/library_tab_layout"
            style="@style/Theme.Widget.Tabs"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:background="?attr/primaryGrayBackground"
            android:focusable="true"
            android:paddingHorizontal="5dp"
            app:layout_scrollFlags="noScroll"
            app:tabGravity="center"
            app:tabIndicator="@drawable/indicator_background"
            app:tabIndicatorColor="?attr/white"
            app:tabIndicatorGravity="center"
            app:tabIndicatorHeight="30dp"
            app:tabMode="scrollable"
            app:tabRippleColor="?attr/textColor"
            app:tabSelectedTextColor="?attr/primaryBlackBackground"
            app:tabTextAppearance="@style/TabNoCaps"
            app:tabTextColor="?attr/textColor" />

    </com.google.android.material.appbar.AppBarLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true"
        android:background="?attr/primaryBlackBackground"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- Tab 1: History Section -->
        <FrameLayout
            android:id="@+id/library_history_holder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            tools:visibility="visible">

            <com.unan.nexxxup.ui.AutofitRecyclerView
                android:id="@+id/library_history_recycler"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="40dp"
                tools:listitem="@layout/home_result_grid" />

            <TextView
                android:id="@+id/library_history_empty_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center"
                android:layout_margin="40dp"
                android:text="@string/library_history_empty"
                android:textColor="?attr/grayTextColor"
                android:textSize="16sp"
                android:visibility="gone"
                tools:visibility="visible" />
        </FrameLayout>

        <!-- Tab 2: Favorites Section -->
        <FrameLayout
            android:id="@+id/library_favorites_holder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="visible">

            <androidx.viewpager2.widget.ViewPager2
                android:id="@+id/viewpager"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingBottom="40dp"
                tools:listitem="@layout/library_viewpager_page" />

            <LinearLayout
                android:id="@+id/library_loading_overlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="?attr/primaryBlackBackground"
                android:visibility="gone"
                tools:visibility="visible">

                <com.facebook.shimmer.ShimmerFrameLayout
                    android:id="@+id/library_loading_shimmer"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:layout_gravity="center"
                    android:layout_margin="2dp"
                    app:shimmer_auto_start="true"
                    app:shimmer_base_alpha="0.2"
                    app:shimmer_duration="@integer/loading_time"
                    app:shimmer_highlight_alpha="0.3">

                    <GridView
                        android:id="@+id/gridview"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:clipToPadding="false"
                        android:gravity="center"
                        android:horizontalSpacing="10dp"
                        android:numColumns="3"
                        android:paddingBottom="120dp"
                        android:verticalSpacing="10dp"
                        tools:listitem="@layout/loading_poster_dynamic" />
                </com.facebook.shimmer.ShimmerFrameLayout>
            </LinearLayout>
        </FrameLayout>

    </FrameLayout>

    <!-- Invisible placeholder frame keeping original view references to prevent binder or code crashes -->
    <FrameLayout
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone">

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/library_random"
            style="@style/ExtendedFloatingActionButton"
            android:text="@string/home_random" />

        <ImageView
            android:id="@+id/library_random_button_tv"
            android:layout_width="0dp"
            android:layout_height="0dp" />

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/sort_fab"
            style="@style/ExtendedFloatingActionButton"
            android:text="@string/sort" />
    </FrameLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
