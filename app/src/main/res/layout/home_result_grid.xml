<?xml version="1.0" encoding="utf-8"?>

<!--  android:layout_width="114dp"
        android:layout_height="180dp"-->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/background_card"
    android:layout_width="160dp"
    android:layout_height="240dp"
    android:layout_margin="10dp"
    app:cardBackgroundColor="@color/obsidian_card"
    app:cardCornerRadius="24dp"
    app:cardElevation="8dp">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/search_poster_img_des"
        android:duplicateParentState="true"
        android:foreground="@drawable/background_shadow"
        android:scaleType="centerCrop"
        tools:src="@drawable/example_poster" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:padding="14dp">

        <TextView
            android:id="@+id/imageText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:fontFamily="@font/google_sans"
            android:maxLines="2"
            android:textColor="@color/text_high_emphasis"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="The Perfect Run" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/episode_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/google_sans"
                android:textColor="@color/text_medium_emphasis"
                android:textSize="12sp"
                tools:text="EP 12" />

            <View
                android:layout_width="4dp"
                android:layout_height="4dp"
                android:layout_marginHorizontal="6dp"
                android:background="@drawable/circle_shape"
                android:backgroundTint="@color/text_medium_emphasis" />

            <TextView
                android:id="@+id/text_rating"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/google_sans"
                android:textColor="@color/cinema_gold"
                android:textSize="12sp"
                android:textStyle="bold"
                tools:text="7.7" />
        </LinearLayout>
    </LinearLayout>

    <FrameLayout
        android:id="@+id/watch_progress_container"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="center"
        android:visibility="gone">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/search_item_download_play"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/obsidian_glass"
            android:padding="10dp"
            android:src="@drawable/ic_baseline_play_arrow_24"
            app:shapeAppearanceOverlay="@style/circle_shape"
            app:tint="@color/white" />
    </FrameLayout>

    <!-- Essential IDs for existing logic (Hidden) -->
    <TextView android:id="@+id/text_quality" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/text_is_dub" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/text_is_sub" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/text_flag" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/watchProgress"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

</androidx.cardview.widget.CardView>
